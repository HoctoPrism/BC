<div class="col-9 d-flex justify-content-between align-items-center">

    <a href="/" class="fs-5 text-secondary">ACCUEIL</a>

    {% for animal in categories %}
        {% if animal.idcategoryparent is empty %}
          
            <a class="fs-5 text-secondary" href="/category/{{ animal.idcategory }}">{{ animal.nameCategory|upper }}</a>
            
        {% endif %}
    {% endfor %}
                
</div>
